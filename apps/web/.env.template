# Application Configuration
NEXT_PUBLIC_APP_ENV=dev                      # Environment: dev or prod

# RainbowKit Wallet Configuration
NEXT_PUBLIC_WC_PROJECT_ID=a8f308de7f5161a288eb0fdadddb16e6

# Upstash KV Storage
UPSTASH_REDIS_REST_URL=https://...
UPSTASH_REDIS_REST_TOKEN=...

# NextAuth Configuration
# NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_URL=https://xyz123.ngrok-free.app
NEXTAUTH_SECRET=your_secret_here  # Generate with: openssl rand -base64 32

# Self Protocol Human Verification
NEXT_PUBLIC_SELF_SCOPE=gigentic                    # Scope name for Self Protocol (max 10 chars)
NEXT_PUBLIC_SELF_APP_NAME="Gigentic CheckPay"        # Display name in Self app
NEXT_PUBLIC_SELF_ENDPOINT=https://xyz123.ngrok-free.app/api/self/verify  # Verification endpoint (ngrok for testing)
NEXT_PUBLIC_SELF_RPC=https://forno.celo.org       # Celo RPC endpoint for Self Protocol

################################
# Chain configurations

# Local deployment (Hardhat)
# NEXT_PUBLIC_CHAIN=hardhat                   
# NEXT_PUBLIC_MASTER_FACTORY_ADDRESS=0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512  # Update after deploying
# NEXT_PUBLIC_CUSD_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3          # Update after deploying MockCUSD
# NEXT_PUBLIC_CHAIN_ID=31337                                                   # Hardhat local
# ADMIN_WALLET_ADDRESS=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266              # Hardhat first account

# # # Sepolia Configuration
NEXT_PUBLIC_CHAIN=celoSepolia                                                    # Chain: hardhat, celoSepolia, celo
NEXT_PUBLIC_MASTER_FACTORY_ADDRESS=0xcontractaddress                             # Deployed MasterFactory contract address
NEXT_PUBLIC_CUSD_ADDRESS=0xdE9e4C3ce781b4bA68120d6261cbad65ce0aB00b              # cUSD on Sepolia testnet
NEXT_PUBLIC_CHAIN_ID=11142220                                                    # Celo Sepolia testnet
ADMIN_WALLET_ADDRESS=0xdeadbeef                                                  # Server-side admin arbiter wallet address (REQUIRED for API authorization)

# # # Celo Mainnet Configuration
# NEXT_PUBLIC_CHAIN=celo                                                           # Chain: hardhat, celoSepolia, celo
# NEXT_PUBLIC_MASTER_FACTORY_ADDRESS=0xcontractaddress                             # Deployed MasterFactory contract address
# NEXT_PUBLIC_CUSD_ADDRESS=0x765de816845861e75a25fca122bb6898b8b1282a              # cUSD on celo mainnet
# NEXT_PUBLIC_CHAIN_ID=42220                                                       # Celo mainnet
# ADMIN_WALLET_ADDRESS=0xdeadbeef                                                  # Server-side admin arbiter wallet address (REQUIRED for API authorization)

